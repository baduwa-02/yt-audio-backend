# YouTube Audio Extractor API

A lightweight Node.js Express API built with `youtubei.js` to extract YouTube music metadata and direct `.m4a` audio stream links.

## API Endpoints

### 1. Search Songs
`GET /api/search?q={query}`

### 2. Get Audio Stream URL
`GET /api/stream/{video_id}`
